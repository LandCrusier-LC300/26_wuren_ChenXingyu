近期课程及课外（比赛）任务较多，因此迟交
1.启发式算法网址：https://github.com/perrygeo/simanneal/blob/master/examples/salesman.py
在进行启发式算法代码的解析过程中，我了解到我所分析的启发式算法叫做模拟退火算法（和我最近学的专业课很契合）。退火过程中，前期可以设定一个很高的温度，但随着退火过程的进行，温度慢慢降低，达到一个稳定的，适合的温度。放在这个算法里面，我的目前理解是前期可以大胆尝试，随着不断的探索，慢慢变得稳定和谨慎，最后获得一个较为优良的解。
2.在训练加州房价预测模型和鸢尾花分类模型时，通过AI，我了解到这两个模型是经典模型，其数据集都可以通过sklearn来获得。同时，两个模型训练的代码实现也很相似，都是先加载数据集，再对数据集进行划分（分为训练集和测试集），然后基于sklearn里已经准备好的模型框架（线性回归模型等）进行模型训练，最后是模型评估。两个模型的不同在于，一个是线性回归模型，一个是分类模型，因此在模型评估上具有差别
3.最后是kmeans聚类代码的补充。我对kmeans聚类算法的理解如下：要对几簇点按聚集程度分类，也就是把他们按距离分为几簇。先随机选择几个中心，对每个中心对应的簇根据距离将点划分到不同的簇，在选定新的中心。如此往复，直到中心不再变化为止。由于缺乏代码撰写的思路，该部分作业我较多地参考了DeepSeek,最后得到的代码运行结果也不是特别好。计划近期任务完成后，对本部分内容进行修正，同时深入理解代码含义，串通代码之间的联系，并尝试再次进行代码编写。